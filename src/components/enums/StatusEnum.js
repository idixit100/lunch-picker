const IDLE = "idle";
const PENDING = "pending";
const REJECTED = "rejected";
const RESOLVED = "resolved";

const statusRange = {
  IDLE,
  PENDING,
  REJECTED,
  RESOLVED
}

export default statusRange;